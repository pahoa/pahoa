pivotaltracker:
  token: ""
board:
  limits:
    in-qa: 1
  transitions:
    - to: "todo"
      actions:
        - "UnstartCard"
    - from: "todo"
      to: "started"
      actions:
        - "StartCard"
    - from: "started"
      to: "waiting-code-review"
      actions:
        - "CreateMergeRequestToDevelop"
    - to: "in-code-review"
    - to: "in-development-test"
    - to: "waiting-qa"
    - from: "waiting-qa"
      to: "in-qa"
    - to: "delivered"

gitlab:
  token: ""
  url: ""

